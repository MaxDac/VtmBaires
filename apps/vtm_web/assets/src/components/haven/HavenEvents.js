// @flow

import React from "react";

const HavenEvents = (): any => {
    return (
        <h1 style={{
            fontFamily: 'Disturbed',
            marginRight: "20px"
        }}>
            Eventi nel Dominio
        </h1>
    );
}

export default HavenEvents;
